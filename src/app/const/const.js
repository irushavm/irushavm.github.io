define([
    "./const.bio",
    "./const.menu",
    "./const.work",
    "./const.vol",
    "./const.proj",
], function (bio, menu, work, vol, proj) {
    return {
        bio: bio,
        menu, menu,
        work: work,
        vol, vol,
        proj: proj
    };
});